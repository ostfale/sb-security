## Own InMemoryUserDetailsService

+ use own implementation of UserDetailsService and send GET request

````shell
curl -u max:test1234 http://localhost:8080/hello                    # provides secured response
``